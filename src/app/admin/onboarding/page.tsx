import { Pipeline } from "@/components/admin/Pipeline";

/** Inbound onboarding submissions, promoted into accounts. */
export default function OnboardingAdminPage() {
  return <Pipeline />;
}

export const dynamic = "force-dynamic";
